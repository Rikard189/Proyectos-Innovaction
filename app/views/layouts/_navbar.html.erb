<%= stylesheet_link_tag "application" %>

<nav class="navbar navbar-default">
  <div class="container container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <!-- <a class="navbar-brand" href="#">Proyectos Interdisciplinarios</a> -->
      <%if user_signed_in? %>
      <%= link_to "Proyectos Interdisciplinarios", home_signin_path, class: 'navbar-brand'%>
      <%else%>
      <%= link_to "Proyectos Interdisciplinarios", root_path, class: 'navbar-brand'%>
      <%end%>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="navbar-text pull-left">
        <%= link_to "Proyectos", root_path, :class => 'navbar-link' %>
    </div>

  <div class="navbar-text pull-right">
    <% if user_signed_in? %>
    <%= link_to "Mis Proyectos", my_proyectos_path, :class => 'navbar-link' %> |
    <%= link_to current_user.name + " " + current_user.last_name, edit_user_registration_path, :class => 'navbar-link' %> |
    <%= link_to '<span class="glyphicon glyphicon-log-out" aria-hidden="true"></span>'.html_safe, destroy_user_session_path,clase:'btn btn-default', method: :delete, :class => 'navbar-link'  %>
    <% else %>
    <%= link_to "Registarse", new_user_registration_path, :class => 'navbar-link'  %> |
    <%= link_to "Iniciar Sesion", new_user_session_path, :class => 'navbar-link'  %>
    <% end %>
  </div><!-- /.container-fluid -->
</nav>
